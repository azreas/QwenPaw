apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "qwenpaw.fullname" . }}-config
  labels:
    {{- include "qwenpaw.labels" . | nindent 4 }}
data:
  QWENPAW_PORT: {{ .Values.service.port | quote }}
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
